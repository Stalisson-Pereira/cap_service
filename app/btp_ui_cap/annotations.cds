using CatalogService as service from '../../srv/cat-service';

// Annotations moved to app/common.cds to avoid duplication and promote reuse.
// Specific annotations for this app can be added here if they differ from common.
